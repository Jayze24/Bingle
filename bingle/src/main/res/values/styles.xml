<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.TranslucentActivity" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">#33638745</item>
    </style>


    <style name="Tile" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">@dimen/size_board_block</item>
        <item name="android:layout_height">@dimen/size_board_block</item>
    </style>

    <style name="Tile.basic" parent="Tile">
        <item name="android:src">@drawable/board_basic_tile_selector</item>
    </style>
</resources>
